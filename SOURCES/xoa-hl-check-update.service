[Unit]
Description=Check for xoa-hl updates

[Service]
Type=oneshot
ExecStart=/usr/libexec/xoa-hl/check-update.sh
# --refresh refetches every repo's metadata; on a slow link that outlasts
# DefaultTimeoutStartSec (90s) and the check would be killed mid-fetch.
TimeoutStartSec=600
